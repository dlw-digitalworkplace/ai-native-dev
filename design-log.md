# AI-native dev flow — Design Log
Last updated: 2026-07-07
Status: **Plan phase, build phase, and dreaming phase functionally designed** (D1–D21), plus a one-time **project onboarding** step that bootstraps the config layer (D18); **all open questions resolved or descoped** (one dreaming sub-item — human-decider emission — deliberately deferred). **Near-term scope: manual execution only** (D6 amendment, 2026-06-24) — unattended automation and the service-identity question are descoped for now. Remaining work is in the parking lot (deployment, etc.) and the deferred-until-automation items, not gaps in the design. **D19→D20 (2026-06-26):** D19 trialed intake & planner as cold subagents; D20 reverted them to in-session slash commands after live testing (per-role model is achieved via per-session model choice). Net: they remain slash commands; cold subagents are reserved for the independent checks. **D21 (2026-06-29)** adds the plan-revision loop — a re-run of `/aind:plan` folds the plan PR's review feedback into the same PR. **D22 (2026-06-30)** adds **GitHub Copilot CLI as a second host** — one repo, no behavior fork (same commands/skills/scripts); a dual manifest + per-tool hooks absorb the only real incompatibility, with a bash-on-PATH prerequisite on Windows. **D23 (2026-06-30)** enriches the **plan document template** — adds a Keep-it-simple/non-goals section, conditional data contracts, a dependency-ordered task breakdown whose tasks cite the project rule files they must obey, a Considerations section, and a sourced Definition-of-done checklist — keeping the planner domain-agnostic (tasks cite `rules/*.md`, never hardcoded domains) and biased toward the simplest change; planning **and** the plan-approval path (`/aind:approve-plan`) are now live-validated. **D24 (2026-06-30)** opens the **build phase**: it packages the coding agent as a warm in-session command `/aind:implement` (per D20 — it authors, it is not an independent check), runs a single rule-driven coder (D23) with polish as its final in-context phase (D7), and scopes the **first iteration to ground→implement→polish→open the code PR** — all test authoring deferred (D8/D9), and code review, the merge gate, and the terminal tag (D7 Phase 4 / D13) held for the next iterations. **D25 (2026-06-30)** keeps AIND stack-agnostic by ruling that **generic, reusable developer guidance** (SOLID, .NET Core usage, house style, …) ships **not in AIND** but in a separate **companion dev-standards plugin** (skills), consumed via D11's two-layer pattern — the project's `rules/*.md` *reference* a standards skill and add their own exceptions/"done" bar — and wired by onboarding (D18). **D26 (2026-07-01)** builds the **code reviewer** (build-phase Phase 4): a **cold reviewer subagent** (`agents/reviewer.md`, the first `agents/` occupant) driven from **inside `/aind:implement`** — after the code PR is opened it spawns the reviewer, the **warm coder** fixes/rebuts, and it re-spawns up to **3 passes**; the reviewer checks the diff against the merged plan **and the full project rule + skill set** with a deliberately strict **CRITICAL+WARNING-block** gate (only SUGGESTION is non-blocking), posts resolvable PR threads + a summary, and never authors fixes; the tag stays `In implementation` throughout, a 3-pass deadlock escalates to a human (PR summary + signed ADO comment, tag unchanged), and **scope ends at reviewer-approval/tiebreak** — merge, the terminal tag, and all test authoring remain the next iterations. **D31 (2026-07-07)** adds a **greenfield sibling to onboarding — `/aind:kickstart`** — which bootstraps the same `.claude/` config from a guided conversation (the three lenses + operational config, plus any design docs) when there is no code to scan; identical gate and config-layer boundary to D18, a warm command reusing D18's tooling (no new scripts), with one greenfield-specific rule: an undecided point becomes a visible `TODO`, never a fabricated convention (`/aind:onboard`, re-run once code exists, reconciles the drafts).

> Decisions are ordered by where they sit in the dev flow (overall shape → intake → status → planning → execution), not by when they were made.
> The **how-it-works** documentation lives in `design-doc.md`; this log holds decisions, rationale, and open items only.

---

## Decisions made

| # | Area | Decision | Rationale | Date |
|---|------|----------|-----------|------|
| D1 | Overall flow + loop-back | **Intake (story gate) runs FIRST; human plan review (plan gate) runs LAST.** Rejections route by **root cause**, not always back to the story: bad story → edit & re-intake; plan-level gap → planner revises *in the PR*; story problem found at plan review → back to intake **and close the stale plan PR**. | The intake gate must be unskippable and sit before any planning cost. Routing by root cause avoids over-rotating to the story for every plan failure. | 2026-06-17 |
| D2 | Intake (phase 0) | An **intake agent** scores each user story *before* planning against a readiness rubric, writes its reasoning in ADO comments. It **suggests** fixes but does **not author** story content — the human authors the story/AC. | Catches vague stories before they burn downstream agent cycles; avoids polished-but-wrong AI-authored requirements. | 2026-06-17 |
| D3 | Status vehicle (cross-cutting) | Express status via **ADO tags** for v1 (not a custom field or the State machine), namespaced under `AIND` (AI Native Dev) — see D4. | Custom-field/process customization may be unavailable to the team; tags need zero setup. Namespacing makes them filterable and collision-free. **Known limitation:** tags are additive and uncontrolled — handled by the single-tag rule in D4. | 2026-06-17 |
| D4 | Status scheme (cross-cutting) | A single **`AIND status - <state>`** tag tracks the coarse phase. **Exactly one AIND-status tag per item at any time; every transition = remove-old-then-add-new, atomic.** States: `Ready for intake` / `Intake declined` / `Intake approved` / `Generating plan` / `Plan ready for review` / `Ready for implementation` / `In implementation` / `Implementation complete`. ADO tag = coarse phase; each GitHub PR (plan PR, then code PR) owns its fine-grained iteration (no PR-review states mirrored into tags). `Generating plan` and `In implementation` are kept as progress/diagnostic flags with a staleness timeout. Human-set: `Ready for intake`, `Ready for implementation`; agent-set: intake/plan states plus `In implementation`; set on merge: `Implementation complete` (mechanism resolved in D13). | Namespaced, queryable, respects request/assert. Coarse/fine split prevents two drifting sources of truth. The diagnostic in-progress flag aids trigger debugging (Q2). | 2026-06-17 |
| D5 | Planning / plan review | The implementation plan is reviewed as a **GitHub PR** (markdown in-repo), not an ADO attachment/comment. The PR is the review surface — inline comments, request-changes, revision diffs. **Refinement (2026-06-24): the planner's assumptions and open questions are recorded in two places and gate the merge.** Each assumption/open question goes (a) under the **Assumptions & open questions** heading in the plan markdown (self-contained, survives in the merged living doc) *and* (b) as an **individual resolvable PR review thread**. With the plan PR's target branch set to **require conversation resolution before merging** (a one-time repo-setting prerequisite), every assumption/open question **must be explicitly resolved by the human before the plan can be merged**. The mechanism is specifically *review threads* — plain top-level PR/issue comments carry no resolve state and would not gate merge. | PR review UX is built for iterating on a structured doc; ADO comments/attachments are not. **On the refinement:** putting each assumption in its own resolvable thread converts it from passive documentation into an active per-item checklist the reviewer must clear — a silent merge is, by construction, impossible while any thread is open, so the human cannot accidentally wave through an unexamined assumption. Keeping the heading in the doc too means the merged plan stays self-contained as living documentation (D10) rather than forcing a reader to reconstruct the assumptions from resolved threads. The branch-protection rule is called out as a setup prerequisite because the agent only *posts* the threads; the *gate* is enforced by the repo setting, not the agent — and it works under current manual scope because branch protection blocks a human's manual merge button just as it would an automated one. | 2026-06-17 (refined 2026-06-24) |
| D6 | Triggers / execution (cross-cutting) | **Execution matures in stages, all sharing one `.claude` config and the same agent definitions — only the trigger and identity change.** **v0 (local):** run each handoff from the Claude Code CLI (`/intake`, `/plan`, or `claude -p`) on a developer machine, acting as the developer's own ADO/GitHub credentials — no service identity, no CI — to prove the agents before any infra; plan-review revisions are re-run by hand. **v1 (GitHub Actions):** agents run via the official `claude-code-action`, kicked off by `workflow_dispatch` (human passes the work-item ID); plan-review revisions are event-driven via PR / `@claude` events (status unchanged); agents read/write ADO via a service identity; no service hooks, orchestrator, or staleness sweep. **v2 (pre-unlocked — service-hook admin confirmed):** swap the manual `workflow_dispatch` for an ADO **service hook → GitHub `repository_dispatch`** keyed on tag transitions (`Ready for intake` → intake; `Intake approved` → planner), plus a scheduled staleness sweep — same workflows, tag-driven. **The build phase (D7) extends the same model unchanged:** v1 `workflow_dispatch` kicks the coder (implement → polish → open PR) and the reviewer is PR-event-driven exactly like plan review; v2 keys the coder off the `Ready for implementation` → build transition. **AMENDMENT (2026-06-24): near-term scope is manual execution only.** The staged target above stands as the intended trajectory, but the *current* scope is deliberately narrowed to **manually-run execution** — i.e. v0 (local CLI, developer's own credentials) is the working mode for now. **v1's unattended-automation aspects are descoped:** the service-identity decision (single bot vs. per-agent, PAT vs. Entra SP, secret storage) is explicitly **out of current scope** and the question that covered it (formerly Q7) is **closed by descoping** — to be reopened when GitHub Actions automation is actually picked up. v2 (event-driven service hooks) is likewise future work. Nothing about the agent definitions, the flow, or the state model changes; only the automation/identity layer is shelved. | Local CLI is the lowest-setup way to validate the agents first; human-triggered GitHub Actions is the cheapest shared path (zero bridge infra), and the genuinely GitHub-native part — plan-PR iteration — gets real automation for free. Because every mode shares one config, promotion is zero change to agent logic; only trigger + identity move. Tags stay the state record throughout (D4), so v2 keys off them with no rework. Claude Code is first-party on GitHub Actions; ADO has no first-party task. **On the amendment:** scoping to manual execution removes the only thing that needed a service identity (an unattended agent), so the identity/secrets question stops being a prerequisite and can be parked without blocking progress — proving the agents by hand is the priority, and the automation layer is a clean later addition that touches trigger + identity only. | 2026-06-17 (amended 2026-06-24) |
| D7 | Build phase (coder → review) | **Once the plan PR merges, the build phase runs entirely in a code PR.** A **coding agent** implements the spec (`In implementation`); a **polish agent** (warm — shares the coder's context) does in-context cleanup (style, formatting, self-consistency) — not an independent check, by design. The coder opens the **code PR**; **CI** owns the objective gates. A **reviewer agent** — *cold: separate invocation, re-grounded only from PR diff + spec + project rules, never the coder's transcript* — reviews spec alignment + missed edge cases. Coder↔reviewer iterate in the PR for **≤3 reviewer passes** (a pass = reviewer comments → coder fixes or rebuts in-thread); the loop **exits early on reviewer approval**. Unresolved after the 3rd pass → **a human reads the threads and posts a verdict; the coder executes it; one final reviewer pass; then merge**. None of this loop moves the tag (coarse/fine split, D4) — the item stays `In implementation` until merge → `Implementation complete`. Coder and reviewer are each modelled as a single agent here, but either may be split into skill-specific agents per project. | Mirrors the plan-phase pattern: fine iteration in the PR, coarse state in ADO. The polish/reviewer split avoids paying twice for one checklist — warm polish does what the coder's context makes cheap (style, consistency); cold review is the genuine independent gate (spec, edge cases). Independence is enforced structurally, not by instruction. The cap with thread-visible disagreement hands the human the real exchange, not a synthesized summary; human-as-tiebreaker (verdict, coder executes) keeps the "agents do the work" pattern intact. | 2026-06-18 |
| D8 | Testing — TDD (point 1 of 2) | **Optional test-first step in the build phase, sitting between `Ready for implementation` and the coder.** **Trigger:** the **planner decides** whether a story warrants spec-level tests and records it in the plan; the human ratifies it at plan review (no new tag — rides the existing human gate). **Who writes what:** a **test-writer agent** — *cold, independent, same re-grounding contract as the reviewer (D7): re-grounded only from the merged spec + plan, never the coder's context, so it cannot be the same running session as the coder* — writes **failing behavioral / acceptance-level tests** (input→expected-behavior, tied to ACs, structure-agnostic). It does **not** write unit tests. The **coding agent** writes **unit-level tests** (warm) for the seams it creates, during implementation. **Review surface:** the behavioral suite is reviewed by a human **before implementation starts**, *inside* the implementation phase — **no separate PR and no new status** (coarse/fine split, D4: the fine-grained test review lives in the phase, the tag stays `In implementation`). **Reviewer's job shifts but does not shrink:** where a behavioral suite exists, spec-conformance is partly enforced mechanically (red→green), so the **reviewer (D7) refocuses on what tests can't express** — coding style, auth, logging, and other cross-cutting/project-rule concerns — plus the spec edge cases the suite missed; when no suite was written, the reviewer remains the sole spec-correctness check. **CI:** the test gate (previously empty) now runs the behavioral suite + the coder's unit tests. **Point 2 (live/E2E testing) is handled separately in D9.** | TDD's value is an *independent* executable encoding of the spec; a coder writing its own spec-tests is a mirror, not a gate (same failure mode as a warm reviewer), so the spec-test author must be cold. Behavioral (not unit) level is the test-writer's natural altitude from a *frozen spec*: unit tests presuppose implementation seams that don't exist pre-code, so a cold agent writing them would secretly design the code, constraining the coder in ways the human never reviewed — and you can't meaningfully review unit tests for code that doesn't exist (the reason the pre-impl review choice forces behavioral level). The hybrid (cold behavioral + warm units) puts independence where it counts and warmth where it's cheap, and gives full coverage overall. Keeping it optional avoids spending an agent on trivial stories; planner-decides-human-ratifies reuses the existing judgment gate instead of a lone machine tag. This finally puts something behind CI's test gate, so the cold reviewer is no longer the *only* spec-correctness check (the gap flagged in D7 / parking lot). | 2026-06-24 |
| D9 | Testing — live / E2E (point 2 of 2) | **Optional, pre-merge, blocking live/end-to-end gate in the build phase, on the code PR.** **Where:** pre-merge and blocking — runs on the code PR alongside CI and the reviewer; a failure blocks merge and routes back to the coder like any other failing gate. Stays at the same altitude as the rest of the build gates, so the terminal state is unchanged (`Implementation complete`) and nothing spills into the (still out-of-scope) deployment phase. **Trigger:** **planner-gated, exactly like D8** — the plan recommends whether the story warrants live testing; the human ratifies at plan review. A story now carries **two orthogonal test recommendations** (spec-level D8, live D9): either, both, or neither. **Who:** a **cold E2E agent** — same re-grounding contract as the test-writer (D8) and reviewer (D7): separate invocation, artifacts only, never the coder's context — authors and runs **user-visible behavioral scenarios** against the ACs (the full-stack altitude of D8's pattern). **Project escape hatches (by design):** the *how-to-run-the-app* mechanics are left to each repo because they are inherently per-app. A project may (a) implement automated E2E the cold agent drives, or (b) **skip the agent and have a developer run the app locally and test manually**, signalling the pass in the PR like any other human input. The design mandates *that the gate is cleared when the plan calls for it*, not how. **Status & CI:** no new status — the gate lives inside `In implementation` (coarse/fine split, D4), and a manual pass is just a human PR signal (no new machinery). The gate is "live testing happened," **not** "the CI E2E job is green": on the manual path there is no CI E2E job at all. | A blocking gate is the only version that is a real gate; advisory E2E that is often red gets ignored. Post-merge E2E would push the first live check past the terminal state into deployment territory that isn't designed yet, and would need a revert story — pre-merge keeps the gate honest and self-contained. Forcing a *running app* into every PR is exactly the per-app complexity worth refusing to pin down centrally, so the two escape hatches (automate it, or a human runs it locally) let each project choose its cost. Cold + behavioral + planner-gated keeps point 1 and point 2 a single coherent pattern — cold behavioral testing at two altitudes (unit-ish in D8, full-stack here) — rather than two unrelated bolt-ons. Signalling a manual pass in the PR reuses the existing human-in-PR pattern instead of inventing a manual-test checkpoint. | 2026-06-24 |
| D10 | Plan PR shape | **Separate plan-PR, merged to the project's integration branch first.** The plan is its own PR on its own branch and **merges to whatever branch the project treats as its integration/working trunk** (e.g. `main`, `develop`, or a `release/x.y` branch — the branch name is deliberately left to each project's branching strategy) **before any code branch exists**; the code PR comes later on a separate branch and targets the same integration branch. **Not** same-branch (plan + code co-evolving on one branch/PR). The plan markdown lives at **`/plans/<work-item-id>/plan.md`**, and it is **permanent living documentation** — it stays in the repo after the code ships, not a throwaway. This makes "the merged spec" that D7/D8/D9's cold agents re-ground from a concrete, stable, addressable artifact. | The entire build phase rests on a **frozen spec**: the cold reviewer (D7), cold test-writer (D8), and cold E2E agent (D9) all re-ground "from the merged spec," which only means something if the spec is genuinely merged and immutable before build begins. Same-branch would make the spec a moving target on the branch and turn the plan gate soft. The only cost — a second PR per story — is cheap next to the independence guarantee it buys. The branch name is left open because branching strategy is per-project (trunk-based, git-flow, release branches) and pinning it centrally would impose a workflow we don't own; the design only requires that the plan merges to the integration branch *first*. A stable `/plans/<id>/plan.md` path gives every cold agent a deterministic re-grounding source; keeping it as living docs means the spec stays discoverable next to the code it produced. | 2026-06-24 |
| D11 | Readiness rubric (intake / phase 0) | **The intake agent (D2) scores stories against a two-layer, hybrid rubric stored at `.claude/intake-rubric.md`.** **Hybrid structure:** *objective* criteria are pass/fail — any miss → `Intake declined`; *judgment* criteria are raised by the agent as advisory comments on the work item, not hard fails (matching D2: the agent suggests, it does not author). **Two layers, single seeded file:** the flow ships a starter `.claude/intake-rubric.md` containing the baked-in **core**; each project edits that file in place to extend it, adding both objective *and* judgment criteria for project-specific needs (e.g. "touches payments → must cite PCI checklist"). **Core is a strong default, NOT an enforced floor** — because it is a single editable file (not a read-only merged layer), a team *can* delete core items; nothing structurally prevents it. **Baked-in core — objective (pass/fail):** title present and non-trivial; ≥1 acceptance criterion exists; intent is stated (user-story form *or* a clear problem/goal statement — "As a…" not forced rigidly); no unresolved placeholders (`TODO`/`???`/`TBD`) in body or ACs; dependencies/blockers named or explicitly "none". **Baked-in core — judgment (advisory comments):** ACs are testable/observable, not subjective ("works well"); story is single-sized, not an epic in disguise; story is internally coherent (title, intent, ACs agree); enough context for a planner to act on the *what* without guessing (the *how* is the planner's job). | Intake is the first and only unskippable gate (D1); without explicit criteria, `approved`/`declined` is noise and the human re-litigates every call, defeating the gate. Hybrid puts determinism where auditability matters (objective checklist) and judgment where a checkbox can't catch the real failure — a well-formatted but meaningless AC. Two-layer mirrors the existing shared-config-plus-project-rules pattern: a core so no project ships with *no* rubric, an extension so teams encode their own context. Single seeded file (Option A) chosen over an enforced read-only core (Option B) for simplicity and to trust teams — the accepted cost is that the core is a default, not an invariant; if enforcement is later needed, promote the core to a read-only merged layer. The rubric lives in-repo so the intake workflow checks it out with the rest of `.claude`, and so changing scoring criteria never touches agent logic. | 2026-06-24 |
| D12 | Exception handling — stuck-state protocol (cross-cutting) | **One generalized stuck-state protocol covers every "agent cannot make progress" failure: planner can't produce a viable plan after N tries, coder can't get the behavioral suite green after N tries, and a merge conflict from concurrently landed work.** Mechanism (identical for all three): each retry loop has a fixed **attempt cap** (the actual N is a tuning detail, parked — set the mechanism now, the numbers empirically later); on hitting the cap the agent **stops** — no infinite retry, no merge, no silent exit. It sets a single shared **`AIND status - Needs attention`** tag (replacing the current status as one atomic change, per D4) and posts **what it tried and why it is stuck** as a work-item comment (planner / coder) or PR comment (coder-in-PR), so the human sees the real trail, not a synthesized summary (same principle as D7). **Merge-conflict special case:** the coder first attempts **one automated rebase/conflict-resolve**; it only escalates to `Needs attention` if that single attempt fails. **Resume:** a human resolves the blocker, then **re-triggers the phase the item was stuck in** — `Needs attention` returns to its origin phase (planner-stuck → re-run planning; coder/merge-stuck → resume build). In **v1 the human picks the right workflow to re-dispatch** (they read the comment and know whether to re-run plan or build); **v2's automatic re-routing from `Needs attention` is deferred** along with the rest of the v2 trigger work. **`Needs attention` is distinct from D7's human-tiebreaker:** D7 (agents *disagree* but are still working) deliberately does NOT move the tag — the item stays `In implementation` and the dispute lives in the PR; `Needs attention` is for an agent that has *stopped and cannot proceed*. | Every agent is triggered and headless — nothing watches it run — so each failure mode needs an explicit "stop and flag a human" exit, or a stuck agent loops forever burning tokens or fails silently while the item sits looking active. Generalizing D7's already-proven shape (cap → stop → escalate with visible context) to one protocol avoids three bespoke mechanisms for one problem. One shared status keeps the coarse/fine split honest (D4): the finer "which failure" is recoverable from the comment, so it doesn't need its own tag and doesn't bloat the state machine. Parking N avoids guessing empirical thresholds pre-implementation. The single automated rebase attempt handles the common trivial-conflict case cheaply without a human, while still escalating the genuine conflicts that need judgment. Returning to the origin phase (rather than a fixed restart point) is correct because the three cases are stuck at different points; v1 leans on the human-as-watchdog already assumed for triggering (D6), so no new routing machinery is needed until v2. Keeping it separate from D7 preserves the meaning of each: "we disagree, keep going" vs. "I am stuck, stop." | 2026-06-24 |
| D13 | Merge mechanics (build phase) | **In current (manual) scope, the merge is human-gated and a single CLI command performs the merge plus the terminal tag-write together.** **Merge trigger:** a person merges the code PR after the reviewer agent approves (or after the D7 human-verdict + final reviewer pass). This is committed as the design position for manual mode, not a stopgap — a human is the final authority on what lands. **Tag write:** the terminal `Implementation complete` tag is written by a **CLI command that also performs the merge** (folded into `/implement`'s final step, or a dedicated `/complete <work-item-id>`), acting as the **developer's own credentials** (the v0 model, D6) — no service identity. **Order and failure handling: merge FIRST, then write the tag.** If the tag-write fails after a successful merge, the item is left **recoverable** — it keeps `In implementation` and the human re-runs the tag step — and is *never* falsely marked complete; the reverse order is forbidden because a `Implementation complete` tag on an unmerged PR would make the state record lie (the discipline behind D4's single-tag invariant). **Explicitly deferred to the automation phase:** auto-merge-on-approval, and a PR-merge GitHub Action that writes the tag as the service identity — both presuppose the unattended machinery and service identity descoped under the D6 amendment (former Q7), so they wait until automation is picked up. | Manual scope (D6 amendment) removes the service identity that the original Q8 assumed, so the realistic answers collapse to human-gated + CLI. Doing merge and tag-write in one command keeps them from drifting — a merged PR with a stale `In implementation` tag is exactly the two-sources-of-truth failure D4 exists to prevent. Merge-first ordering makes the only possible failure a *recoverable, visibly-incomplete* item rather than a *false-complete* one: a lie in the state record is far worse than a missed update, since downstream readers trust the tag. Committing to human-gated (rather than holding it loosely) reflects that even once automation returns, a human merge gate is a defensible permanent choice; auto-merge is offered as a *future option*, not a planned default. Deferring the automation-mode answer keeps Q8 consistent with the Q7 descope instead of designing machinery that isn't in scope. | 2026-06-24 |
| D14 | Behavioral suite shape (build phase) | **The cold test-writer's behavioral suite lives on the SAME branch as the implementation (one PR carries red→green), reviewed in place before the coder writes code — NOT a separate test-PR merged first.** The test-writer (D8) commits the failing behavioral suite as the first commit(s) on the code branch; a human reviews the suite **at the test-only state of the branch** — after the test-writer's commits, before any implementation commits are pushed (per D8: reviewed before the coder starts, inside the implementation phase, no new status). The coder then adds implementation to the same branch, turning the suite green, and the single code PR carries the whole red→green history. The exact PR-opening timing (review the branch at the test-only commit, or open the PR early as tests-only and review its first state) is a mechanical detail left open; it does not change the design. **Guard:** because the suite is *not* merge-frozen (it sits on the branch, where the coder could in principle weaken it), the **cold reviewer (D7/D8) treats any unexplained change to the behavioral test files as a review flag** — re-grounded from the spec, it checks the tests still match the ACs. This closes the "coder weakened the tests" gap that a merged-first test-PR would have closed by freezing. **This deliberately diverges from D10** (which chose separate-PR-merged-first for the *plan*). | The artifact coupling is the opposite of the plan case, so the same-branch-vs-separate-PR answer flips. The plan is *loosely* coupled to code, so D10's merge-freezing adds integrity at low cost. The behavioral suite is *tightly* coupled (same language/repo, designed to go red→green against the code), so separate-PR has high cost and low benefit here: (1) D8's independence is already enforced **structurally** (cold invocation, separate context) — it never depended on branch isolation, so freezing the suite on trunk adds little integrity; (2) merging a **red** suite to the integration branch first would break trunk CI for everyone branching off it, violating the basic "trunk is green" invariant — a real harm, not theoretical; (3) tightly-coupled artifacts want to travel and merge together, so splitting them is the unnatural move (the inverse of the plan, where splitting was natural). The reviewer-flags-test-changes guard recovers the only integrity that same-branch gives up, and it's catchable rather than silent because test-file edits appear in the same PR diff the reviewer already reads. Net: principled divergence from D10, not inheritance. | 2026-06-24 |
| D16 | Dreaming phase — continuous improvement loop (cross-cutting, non-linear) | **A non-linear, cross-story phase that learns from the flow's exhaust and proposes improvements to the agent-config layer.** Four parts. **(1) Emission (push, warm):** each agent emits a structured *lessons-learned* record at the end of its session — e.g. the planner after iterating on plan-PR comments, the coder/polish after a recurring style fix. The agent is the correct *warm* source because only it knows what it tried and why it iterated; this self-report is deliberately warm, exactly the kind of self-grading ruled out for *gating* (polish D7, test-writer D8) but appropriate for *emission*. **(2) Synthesis (cold dreamer):** a separate **dreamer agent** — *cold, same re-grounding contract as the reviewer (D7) / test-writer (D8) / E2E agent (D9): a fresh invocation re-grounded only from the emitted lessons + artifacts, never any agent's running context* — reviews the accumulated lessons and proposes improvements. The split is **reflect-warm / synthesize-cold**, mirroring D7/D8: warmth where it is cheap (an agent self-reporting its own session), independence where it counts (deciding what is a real, recurring pattern versus a one-off). **(3) Cadence:** runs on a regular basis; the framework leaves the exact trigger open — **e.g. every X `Implementation complete` items** (a volume-based example, not pinned, same spirit as parking N in D12). **(4) Output & authority:** every proposal lands as a **GitHub PR against `.claude`**, **one dream cycle = one PR**, which a **human accepts or rejects** — the dreamer *proposes*, the human *disposes*. This human gate is **permanent, not a manual-scope limitation** — the D2/D13 *agent-suggests-human-decides* pattern applied to the highest-blast-radius write in the system. **Authority is hard-bounded to the config / "learned behavior" layer** — skills, agent prompts, the intake rubric (D11), project rules — and **never the flow itself**: the status model (D4), the gates, and the structural decisions D1–D15 are off-limits. The dreamer may *raise* a suspected structural problem as a **parking-lot note for a human**, but must **not** propose structural changes as mergeable diffs. Improving *how* an agent does its job ≠ redesigning *the job*. **Deferred sub-item:** the human decider (D7 tiebreaker) and human merger (D13) are **not agents and do not auto-emit**, so the highest-value datum — *why a human overrode the machine* — is not captured by the emission step; capturing it is deferred (it would add friction to the human gate, and is a gate-mechanics detail, not framework shape). | The whole flow is a system of agents reading `.claude`; until now **nothing closed the loop on improving that config** — a linear pipeline with no feedback path. The dreamer adds that path, but it is structurally the **most dangerous component in the system**: it is the first phase that is asynchronous, cross-story, and **self-modifying** — it *writes the very files every other agent reads*, so a bad lesson silently degrades every future run rather than breaking one story. That blast radius is why it gets the **strictest** controls, not the loosest: the permanent human merge gate (it cannot change agent behavior without a human merging a PR), the cold-synthesis guarantee (warm self-grading must not decide what counts as a pattern), the one-PR-per-cycle shape (each change individually rejectable, auditable, and revertible — D5's resolvable-thread philosophy at the file level), and the hard authority boundary keeping it on the **config side of the D1–D15 line** (a self-improving agent that could rewrite its own gates is exactly the failure to design out). Emission is warm because the agent is the only thing that knows the *why* behind its iterations — artifacts show *that* the planner iterated three times, not *what it learned* — and the dreamer's coldness is precisely what keeps the synthesis of those warm reports honest. | 2026-06-25 |
| D15 | E2E execution environment (build phase) | **In current scope, the live/E2E gate (D9) is satisfied by the manual developer-run path only; the automated E2E path is deferred.** When a story's plan calls for a live test (D9), the gate is cleared by a **developer running the app locally and testing manually, signalling the pass in the PR** — exactly the manual escape hatch D9 already defines. The **automated path** (a cold E2E agent stands up the running app in CI and drives user-visible scenarios) is **not buildable in current scope** and is deferred: it depends on the unattended automation layer (descoped under the D6 amendment) *and* on a way to stand up a running application in a PR/CI context — ephemeral CI service, preview/dev deploy, or containerised run — which overlaps the **deployment phase that is in the parking lot and out of scope for the initial design**. **D9's two-path design stays intact as the target:** when both the automation layer and a deployment/preview story exist, the automated path becomes real and projects choose per-repo as D9 specifies. This decision only names what is actually available today (manual) versus deferred (automated) — it does not narrow D9. | Q10 is entirely about the *automated* path; the manual path needs none of this infrastructure and is already fully specified in D9, so it is the natural — and currently the only available — answer. Stating "manual now, automated deferred" (rather than leaving it nominally "per-project") is the truthful framing: a project *cannot* take the automated path right now because both prerequisites are deferred, so "your choice" would be a false choice in current scope. The blocking dependency that actually adds information is the deployment/preview story (parking lot); the automation dependency is already established by the D6 amendment. Naming the deployment overlap explicitly prevents a premature attempt to spec automated E2E before there is any way to run the app in CI. | 2026-06-24 |
| D18 | Project onboarding (one-time, pre-flow; bootstraps the config layer) | **A human-invoked onboarding agent (`/onboard`) bootstraps a project's `.claude/` config from its existing codebase, as the day-one mirror of the dreamer (D16).** It reads the repo, **discovers the rule areas it actually needs through three lenses** — **technical layers** present (front-end, back-end, web-jobs, infra, …), **cross-cutting concerns** with a notable/non-standard approach (e.g. a custom pin-code auth scheme), and the **functional/domain architecture** (the product's core concepts and invariants, e.g. "app-of-mini-apps; every entity scoped to a couple of IDs") — and **drafts** the initial config: one `rules/<area>.md` per discovered area, a `CLAUDE.md` that imports exactly those files via the root-import model, **project-specific skills** stubbed from the real build/test/run commands it finds (manifests, CI), and a copy of the seed intake rubric (D11). **Refinement (2026-06-25):** the layer names are *examples, not a fixed list*, and discovery is **strictly evidence-only** — a rule file is created only for an area that genuinely exists (no test framework → no testing rule; no docs system → no docs rule), and the three-lens prompt was added so onboarding does not stop at technical layers but also surfaces cross-cutting concerns and the functional/domain architecture. The shipped `project-template/rules/` is a single category guide (`_TEMPLATE.md`), not a set of stub files, so nothing biases the agent toward a specific domain set. It then runs a **preflight probe** and reports the remaining setup prerequisites (PAT, gh access, jq, the Boards↔GitHub integration, branch protection). **Authority & gate:** it **suggests, the human decides** (the D2 pattern) — every generated file is a clearly-marked **DRAFT** for review, and it is **bounded to the config layer, never the flow** (the same D1–D15 boundary the dreamer respects, D16). **Delivery:** draft files written directly into `.claude/` (reversible via git, reviewed before commit) — **not** a PR-against-`.claude`, because the PR-review machinery (D16's one-cycle-one-PR) presupposes a running flow and a configured repo that do not yet exist at onboarding. | Adopting AIND on an existing repo otherwise starts from empty template stubs a team fills by hand; turning the codebase itself into the first draft of its rules is faster and more grounded. Reusing the agent-suggests/human-decides gate (D2) avoids inventing a new trust model, and keeping the onboarder on the config side of the D1–D15 line is the same failure-class avoidance as the dreamer — a setup agent that could rewrite the flow is exactly what that boundary designs out. Draft-files-not-PR is the right call *specifically at onboarding*: the human gate is preserved (review before commit) without the bootstrap chicken-and-egg of needing the flow configured to review its own configuration. Discovering domains rather than fixing them keeps the rule split honest to each repo's real shape, and the root-CLAUDE import model (chosen for project rules) wires in exactly the domains found. Folding the preflight probe into onboarding means the team finishes the command knowing precisely what remains to set up. The onboarder/dreamer symmetry is deliberate: **onboarding bootstraps the config from the codebase, dreaming evolves it from the flow's exhaust** — two ends of the same agent-config-improvement axis, both proposing, both human-gated, both barred from the flow. | 2026-06-25 |
| D31 | Greenfield onboarding — the kickstart agent (pre-flow; sibling of D18) | **A human-invoked `/aind:kickstart` bootstraps a NEW/greenfield project's `.claude/` config from a guided conversation, where D18's onboarding has no code to scan.** It elicits the project across the same three lenses D18 discovers from code — functional/domain, technical layers, cross-cutting concerns — plus the operational config (integration branch, ADO/GitHub targets, intended build/test/run tooling), reading any design docs the user points it at, then **drafts** the same artifacts: one `rules/<area>.md` per **decided** area (three-lens `_TEMPLATE.md` shape), a `CLAUDE.md` importing exactly those files, **placeholder** skills carrying the *intended* build/test/run commands **marked unverified** (the toolchain may not exist yet), and a seed-rubric + env-sample copy. Procedure: orient (redirect to `/aind:onboard` if substantial code is found) → three elicitation rounds → batched/adaptive gap questions (`AskUserQuestion` for enumerable choices, prose for the domain) → **propose the whole `.claude/` tree for validation before writing anything** → write the DRAFT files → preflight → summarize with next steps. **Authority & gate identical to D18:** suggest-don't-assert (every file a clearly-marked GREENFIELD DRAFT), draft-files-not-PR, and **bounded to the config layer, never the flow** (the D1–D15 boundary). **The one greenfield-specific discipline:** because a new project's answers are often *decisions not yet made* rather than observed facts, it **never fabricates a convention to fill a gap** — an undecided point becomes an explicit `TODO`/open question in the draft, not a rule. **Packaging = warm in-session slash command** (D20's line: it authors artifacts a human reviews, so nothing to stay cold from), reusing D18's tooling (`_TEMPLATE.md`, `project-template/CLAUDE.md`, the seed-rubric/env copy, `aind-preflight.sh`) — **no new scripts**. **Complementarity:** kickstart bootstraps config *before* code exists; `/aind:onboard`, re-run once real code lands, reconciles the intended-design drafts against the actual codebase. | D18 turns "adopting AIND on an existing repo" from empty stubs into a grounded first draft — but a greenfield team has the opposite problem: no code, so nothing for onboarding to read, leaving them back at hand-filling stubs. A conversation is the only available source of the same three-lens picture before code exists, and eliciting it is exactly the kind of judgment a warm authoring command is for. Reusing D18's gate (suggest/human-decides), boundary (config-not-flow), and delivery (draft files reviewed before commit) avoids inventing a second trust model for what is structurally the same day-one bootstrap. The non-fabrication rule is the *essential* difference from D18 and the reason it's a distinct decision: onboarding records observed facts, so a rule it writes is grounded by construction, whereas kickstart's inputs are partly unmade decisions — allowing it to invent a convention to look complete would violate suggest-don't-assert and ship a guessed rule every agent then obeys, so undecided must degrade to a visible TODO, not a silent default. Propose-then-write (vs. D18's write-then-summarize) is the right gate here because there's no code to constrain the draft — all judgment — so a cheap validation round before authoring avoids churn. Warm-command packaging follows D20/D24's settled line (author = warm command; cold = independent verification only), and leaning entirely on D18's existing tooling keeps it a single thin command file with zero new mechanics to test — the whole feature is judgment + conversation, which is where commands are supposed to live. | 2026-07-07 |
| D17 | Artifact linking & resolution (cross-cutting) | **How an agent resolves between a work item, its plan, its branch, and its PRs is a defined contract — not left to a naming convention.** Two layers. **(1) Framework convention (the fast path):** the plan markdown lives at **`/plans/<work-item-id>/plan.md`** (D10), and the **work-item ID is the join value** carried in artifacts. **Branch naming is a project responsibility, not framework** — the framework never constructs, assumes, or recognises a branch name; reaching the branch goes *through* the code PR, not by deriving a branch string. **(2) Stored pointers (the source of truth):** the plan PR and the code PR are each **linked to the ADO work item via ADO's native work-item↔PR linking** (the Azure Boards ↔ GitHub integration). **In addition**, each PR body carries a fixed, machine-parseable `AIND-LINKS` block (an HTML comment, invisible in rendered PR text) listing the **work-item URL**, the **plan path**, and — on the code PR — the **plan-PR URL**. Both are kept on purpose: the native link is the canonical pointer and the human-visible one; the in-body block is what lets a **cold agent re-grounding from artifacts only** (the PR diff/body) resolve the work item and plan **without an ADO round-trip**, keeping "re-ground from artifacts" literally true. **Resolution rules:** *forward* (have ID → plan file by the `/plans/<id>/plan.md` convention; ID → PRs by following the native links from the work item); *reverse* (have a PR → work item and plan via the native link and the `AIND-LINKS` block). **Conflict rule:** if a convention-derived guess and a stored pointer disagree, the **stored pointer (native link) wins and the agent flags it** — this is the survived-a-rename / hand-edited case. **Prerequisite (named, like D5's branch-protection rule):** the **Azure Boards ↔ GitHub integration must be connected** for native links to exist; if it is not, the degraded fallback is a fixed-prefix work-item comment (`AIND-LINK: <kind> <url>`). Native is primary, the comment is the fallback. **Failure handling (mirrors D13's merge-then-tag):** the link/back-pointer is created **after** the PR is successfully opened, so a failed link leaves a real, convention-discoverable PR rather than a dangling pointer to nothing — recoverable, never corrupt. **Scope:** entirely v0-CLI-doable under the developer's own credentials; under automation the same links are created by the service identity, contract unchanged (D6). | The whole cold-agent design (D7/D8/D9/D16) rests on agents re-grounding "from the merged spec," but until now nothing said how an agent handed only a work-item ID *finds* the spec, the branch, or the PR — a naming convention was silently load-bearing, and a renamed branch or hand-edited title would break the join with no detection, and there was no reverse pointer at all. A two-layer contract fixes both directions cheaply: the convention stays the fast path, and a canonical stored pointer makes reverse lookups robust and rename-proof. Native ADO links were chosen over greppable comments because they are a built-in link type (not the field/process customization D3 warned might be unavailable), they are human-auditable from the Boards UI, and they need no parsing — with the integration-connected prerequisite called out explicitly so the dependency is visible, exactly as D5 surfaces its branch-protection prerequisite. The in-body `AIND-LINKS` block is kept *as well* because a cold agent's contract is "artifacts only": it sees the PR body but does not necessarily query ADO's link API, so the in-body block is what keeps that contract honest without an extra round-trip. Branch naming is left to the project for the same reason D10 leaves the integration-branch name open — branching strategy is per-project and pinning it would impose a workflow the framework does not own — which is *why* the branch is reached through the PR rather than by deriving a name. Stored-pointer-wins-and-flags makes a rename a *detected, recoverable* event rather than a silent break, and create-link-after-PR-open makes the only possible failure a recoverable-but-discoverable item, the same shape as D13's merge-first ordering. | 2026-06-25 |
| D19 | Role packaging (cross-cutting; config layer) | **(SUPERSEDED by D20, 2026-06-26 — reverted to in-session commands.)** Intake and the planner are re-packaged from in-session slash commands into cold subagents (`agents/intake.md`, `agents/planner.md`), each fronted by a thin command (`/aind:intake`, `/aind:plan`) that does nothing but delegate to its subagent and relay the result.** The subagent owns the full procedure, status transitions, and stuck-state handling; it carries its own `name`, `color`, and — the decisive capability — its own **`model`** (intake and planner may run on different models). The thin command stays the human entry point and the `claude -p` / Actions handle (D6). **This is a packaging change on the *config* side of the D1–D15 line only — the flow is untouched:** the status model (D4), the gates, and the plan-PR mechanics (D5/D10/D17) are unchanged, and signing enforcement is preserved because the `PreToolUse` hook matches the `Bash` tool globally and fires for a subagent's calls exactly as for the session's. **`polish` is explicitly NOT promoted** and stays warm (D7). | The `CLAUDE.md` warm/cold rule conflated two axes: (a) *independence from an upstream agent's hot context* (the real reason the reviewer/test-writer/E2E/dreamer are cold — D7/D8/D9/D16) and (b) *being the human's entry point*. Intake and the planner were classed "warm" only on axis (b); on axis (a) they have **nothing hot to inherit** — intake re-grounds from the story + rubric, the planner from the story + rules + codebase, both already "from artifacts." Their human gate is **asynchronous on an artifact** (intake's signed verdict comment + tag; the planner's resolvable PR threads, D5), not live steering mid-run — the planner is even specified to *never block on a human* (proceed-on-assumption). So their warmth was incidental, unlike `polish`, which is warm for a **real** reason (D7: it deliberately shares the coder's context to avoid paying twice). Three drivers force the move: **(1)** headless/automation — a self-contained ID-in/PR-out subagent is exactly the D6 trajectory; **(2)** redundant warmth — no live steering happens, so in-session buys nothing; **(3)** per-role model selection — a slash command **inherits the session model and cannot override it**, while a subagent carries a `model:` field, so giving intake and the planner different models is *only* possible as subagents. The thin-command-fronts-cold-subagent shape keeps the human entry point and the "commands are thin / mechanics in scripts" invariant intact while isolating each role's context and unlocking the model knob. Keeping `polish` warm preserves the one place warmth is load-bearing. | 2026-06-26 |
| D20 | Role packaging (supersedes D19) | **Revert intake and the planner from cold subagents back to in-session slash commands** (`/aind:intake`, `/aind:plan`); the `agents/intake.md` / `agents/planner.md` subagents are deleted. Per-role model selection — D19's decisive driver — is obtained instead by **choosing the session model per invocation** (`claude --model …` or `/model`), which is natural because intake and planning are separated by the human plan-review gate and are never run in one continuous session. **Cold subagents remain reserved for the genuinely independent checks** (reviewer, test-writer, E2E, dreamer), where independence from the work being checked is the whole point. | Live testing showed D19's drivers did not hold up. Headless runs work fine from a command (`claude -p "/aind:intake <id>"`), so automation never needed a subagent; "redundant warmth" only established that cold was *possible*, not better; and per-role model — the one strong driver — is met more simply by per-session model choice, since the two phases share no session (they are gated apart by human plan review). Against near-zero benefit, the subagent form imposed real friction: a fresh subagent context does **not** inherit the session's permission approvals, so the scripts re-prompted on every run; the command→Task→subagent indirection was less stable; and cold re-grounding cost tokens and latency for roles that *author* artifacts a human then reviews rather than *independently verifying* another agent's work. Industry practice favors the simplest architecture that works — reserve subagents for parallelism, hard context limits, or independent verification — none of which intake/planner need. The revert also sharpens the cold/warm line: **cold = independent verification only; warm command = human-facing authoring**. The fan-out case (orchestrating many stories at once) is out of current manual scope (D6); a subagent can be reintroduced then if that need is real. | 2026-06-26 |
| D21 | Plan-revision loop (implements D1's revise-in-PR path) | **`/aind:plan` is mode-aware: a first run CREATES the plan PR; a re-run on a story that already has an open plan PR enters REVISE mode** — it checks out the existing plan branch, reads ALL the PR's review feedback (top-level comments + every resolvable thread, each tagged `[OPEN]`/`[RESOLVED]` with its `file:line`), folds the open items into `plans/<id>/plan.md`, and commits/pushes to the **same** PR (posting a one-line summary as a PR comment). It never opens a second PR and never moves the status tag (stays `Plan ready for review`); the planner does **not** resolve threads — resolution remains the human's merge gate. New mechanics: `aind-revise-plan-pr.sh` (`status` = detect mode, `begin` = checkout + print feedback digest, `push` = commit/push/comment); `aind-open-plan-pr.sh` now refuses when a plan PR already exists and points to revise mode. **Refinement (2026-06-29):** on a re-run the planner also *replies* on each open thread — a clarifying question when the feedback is unclear, an "addressed — please resolve" note when the revision handled it (`aind-revise-plan-pr.sh reply <thread-id>`, which surfaces each thread's node id in the `begin` digest) — but still **never resolves** a thread, so D5's human-resolution merge gate is untouched. The plan-PR branch lifecycle is closed by a `cleanup` phase that `/aind:approve-plan` runs post-merge (deletes the remote + local plan branch, gated on the PR being `MERGED` so an unmerged plan is never dropped; the plan doc survives on the integration branch per D10). | D1 always specified that plan-level gaps are fixed by the planner *revising in the PR*, and D6's v0 said revisions are "re-run by hand" — but the implementation only supported initial creation, so a re-run conflicted on the branch push and `gh pr create`. This closes that gap and turns the plan PR into a genuine **multi-reviewer feedback loop**: anyone can leave comments / reply to threads, and one re-run ingests them all and updates the single PR in place. Same-branch/same-PR iteration preserves D5 (assumptions as resolvable threads gating merge) and D10 (one living plan doc per story); the agent not resolving threads preserves the human gate; not moving the tag preserves the coarse/fine split (D4). Reading feedback through `gh`'s built-in `--jq` avoids a hard external-`jq` dependency on the GitHub path. Net: D6's "by hand" becomes "one re-run of `/aind:plan`, now revise-aware," still squarely within v0 manual scope (no automation/service identity added). | 2026-06-29 |
| D22 | Second host — GitHub Copilot CLI (cross-cutting; packaging) | **The plugin runs on BOTH Claude Code and GitHub Copilot CLI from one repo, with no behavior fork** — identical `commands/`, `skills/`, and bash `scripts/` (the single source of behavior). Only two things differ per host, both packaging: **(1) Manifest** — `.claude-plugin/plugin.json` (Claude) and `.github/plugin/plugin.json` (Copilot) coexist; each points its own `hooks` field at its own hook file. Copilot prefers `.github/plugin/plugin.json` and ignores `.claude-plugin/`; Claude reads only `.claude-plugin/`. **(2) Hooks** — the one genuine format incompatibility. Claude: `hooks/hooks.claude.json` (`PreToolUse`/`matcher`, command reads `{tool_input:{command}}` on stdin, blocks via **exit 2**) → `check-claude-comment.sh`. Copilot: `hooks/hooks.copilot.json` (`version:1`/`preToolUse`, `bash`+`powershell` keys; reads `{toolName,toolArgs}` on stdin, returns a `permissionDecision` JSON, **never exit 2**) → `check-copilot-comment.ps1`/`.sh`. Both load **only via explicit manifest** — the bare conventional `hooks/hooks.json` is removed so neither tool auto-discovers the other's file. `${CLAUDE_PLUGIN_ROOT}` is unchanged (Copilot injects it alongside `PLUGIN_ROOT`/`COPILOT_PLUGIN_ROOT`). **Prerequisite (Windows):** Copilot's shell tool is PowerShell, so **Git's `bash` must be the `bash` that resolves** in the launching shell — and it must win over Windows' `System32\bash.exe` (WSL launcher), which sits on the machine PATH ahead of any user-PATH entry, so a user-PATH *append* of `Git\bin` is insufficient; **prepend** it (e.g. via `$PROFILE`) and verify with `(Get-Command bash).Source`. Install with `copilot plugin install <owner>/<repo>` (commands namespace identically, `/aind:*`). | Empirically (live testing, Copilot CLI 1.0.65), almost everything ports unchanged: the manifest is read directly, commands register **with their `/aind:` namespace**, scripts are provider-agnostic, and `${CLAUDE_PLUGIN_ROOT}` resolves. The **only** true incompatibility is the hook config schema + I/O contract — so the minimal, honest split is a per-tool hook file behind a per-tool manifest, leaving one behavior layer untouched. Removing the conventional `hooks/hooks.json` (and naming both files explicitly) eliminates the original breakage — Copilot tried to parse the Claude-format `hooks.json` and errored, which (preToolUse errors → deny) blocked *every* tool call. Now nothing is auto-loadable to collide. The bash-on-PATH prerequisite is non-negotiable: without `bash`, the Copilot model either **reimplements the scripts in PowerShell** (silently violating the single-`AIND status`-tag invariant of D4 and the signed-comment rule — observed: duplicate tags + an unsigned comment via ad-hoc `az`/REST) or **gives up mid-run** — both worse than a clean stop. The hook port preserves signing enforcement on the Copilot side; the dual design keeps Claude's working path byte-for-byte. Net: a packaging change on the config side of the D1–D15 line; the flow, status model, and gates are untouched. | 2026-06-30 |
| D23 | Plan document contents (planning / phase 1) | **The plan markdown gets a richer, fixed template so it carries enough for a coding agent to execute without re-deriving decisions — while staying domain-agnostic.** Headings, in order: **Context**; **Keep it simple** *(conditional)* — explicit non-goals plus the simpler option taken over a heavier one, dropped when there is nothing real to fence off; **Implementation approach**; **Data contracts** *(conditional)* — only when the change crosses a boundary (API↔client, service↔service, module↔module), pinning exact field names/types/nullability + any mapping, expressed in the project's own language(s), and **never fabricated** for a change with no boundary; **Task breakdown** — an ordered (dependency-first) task list where **each task cites the project rule file(s) (`.claude/rules/*.md`) it must obey** instead of being filed under a fixed domain bucket; **Assumptions & open questions** (unchanged role, D5); **Considerations** — non-blocking reviewer FYI only; **Testing recommendations** (unchanged, D8/D9); **Definition of done** — a checklist every item of which traces to a real source (an acceptance criterion, a cited rule's "what done looks like" bar, an invariant, or a ratified testing decision); **Files/areas affected**. Supporting changes: the planner also reads **project skills + `docs/`** (not just rules + code) when grounding and respects a multi-project/deployment topology where the rules define one; it is instructed to **bias toward the simplest change that satisfies the ACs** (no speculative abstraction/config/generalization); and the **Considerations-vs-open-questions boundary has a tie-breaker — anything a reviewer could reasonably want done differently is an open question (→ resolvable thread / merge gate), not a consideration; when in doubt, prefer the open question.** Revise mode keeps every new section honest on re-runs. Config side of the D1–D15 line: no flow/status/gate change; D5/D10 (assumptions-as-threads, living plan doc) and D8/D9 (two testing recommendations) are untouched. | The first template (Context / Approach / Assumptions / Testing / Files) proved too thin for a coding agent — it lacked an executable task decomposition, an explicit definition of done to validate against, and the cross-boundary **data contracts** that prevent front-/back-end property-name drift (the failure mode an external reference planner targeted by making data contracts first-class). The improvements adopt those ideas but keep the plugin **domain-agnostic**, mirroring D11's "rubric is data, command is procedure": the command never enumerates domains — tasks **cite** whatever `rules/*.md` the project actually has. Rule-citation beats domain-as-section because tasks order by dependency and some are cross-cutting (a task may cite >1 rule), so binding a task to its rules — not grouping it under one — is what makes the coder apply the right conventions and the right "done" bar; the Definition of done then aggregates exactly those cited rules' bars. The simplicity bias + Keep-it-simple section counter the LLM tendency to over-engineer (live-confirmed: the planner **declined an unrequested shared-nav-config refactor it had previously proposed**, recording it as a non-goal). Conditional sections with "drop rather than fabricate/platitude" stop the richer template from emitting filler on small stories. The Considerations tie-breaker closes a gap found in testing, where a borderline decision with a real alternative (selection-highlight on overflow routes) was filed as a non-gated note instead of a resolvable thread. **Explicitly NOT adopted** from the reference planner (each conflicts with AIND): "ask for clarification / don't make assumptions" (the planner never blocks — proceed-on-assumption + threads, D5); an in-doc `Status:` line (would compete with the single AIND tag, D4); per-task acceptance criteria and a fixed named-agent roster (consolidated into the rule-cited Definition of done; build-phase checks are cold and selected by rule, not named in the plan). Live-validated end-to-end on a real story (AB#19). | 2026-06-30 |
| D24 | Build phase — coding agent packaging & first-iteration scope (build phase, follows D7) | **The coding agent is a warm in-session slash command `/aind:implement <work-item-id>` — not a subagent** (consistent with D20: cold = independent verification only; warm command = human-facing authoring). It authors code that a human and the future cold reviewer check, so it has nothing hot to stay independent *from*. **Single coder, not per-domain:** per-task conventions arrive through each task's cited `rules/*.md` (D23); per-domain splitting stays parked (D7). **Polish (D7) is the final in-context phase of the same command** — warm, shares the coder's context, applies cited-rule style/formatting/self-consistency tweaks only (no structural or behavioral change), then opens the code PR. **Grounding (D17):** ID → resolve the merged plan (`AIND-LINKS` block / the `plans/<id>/plan.md` convention) → read the plan + each task's cited `rules/*.md` + the project's build/run **skills**; build/run mechanics stay **project skills** (D18), never plugin scripts — the plugin adds only the GitHub-flow twin of the plan-PR opener (`aind-open-code-pr.sh`: push branch, `gh pr create`, write the code PR's `AIND-LINKS` block incl. the plan-PR URL, native-link to the work item). **Code-branch naming:** the coder **generates** the branch from a fixed convention **`[type]/<work-item-id>-<short-name>`** (e.g. `feat/123-new-component`, `fix/456-typo`); this does not break D17 — nothing in the *flow* constructs or recognises a branch by name, the agent picks it once and every later agent still reaches the branch *through the code PR*. **State (D4):** `Ready for implementation` → set `In implementation` at start; the code PR owns all fine iteration (no tag movement). **Stuck-state (D12):** cap retries → stop → `Needs attention` → post the trail; a merge conflict gets one automated rebase attempt, then escalates. **First-iteration scope ends at code-PR creation:** all test authoring is deferred (neither the cold test-writer D8 nor the coder's own warm unit tests D7/D8 — D8/D9 stay the target), and code review (cold reviewer, D7 Phase 4), the human merge gate, and the terminal `Implementation complete` write (`/aind:complete`, D13) are explicitly the *next* iterations, not this one. | Packaging follows the warm/cold line D20 finally drew: the coder authors and a human / the cold reviewer verify, so coldness buys it nothing while re-incurring the D19 friction D20 paid to remove (fresh-context permission re-prompts, command→Task indirection, cold re-grounding cost); a command is also the proven headless handle (D6, `claude -p`). One coder honors D7's "modelled as one; split is project-specific" and leans on D23's rule-cited tasks for per-domain conventions, so domain-awareness needs no extra agents. Polish stays warm and in-context because that is the one place warmth is load-bearing (D7) — re-grounding it cold would redo the coder's reading for zero independence gain. Dev skills staying project-side preserves portability (no project values in the plugin) and reuses what onboarding already stubs (D18); the plugin only mirrors the plan-PR opener for the code PR. Fixing the branch convention *in the agent* (not in a flow script) keeps D17's contract intact — the PR remains the only handle — while giving humans a readable branch. Deferring all testing and stopping at PR creation makes this iteration a single reviewable surface (the "name what's actually available now" discipline of D15), with review / merge / complete added as clean follow-ons rather than half-built now. | 2026-06-30 |
| D25 | Generic dev knowledge — companion standards plugin (config side of the D1–D15 line; cross-cutting) | **Generic, reusable developer guidance — SOLID principles, ".NET Core usage", house coding style, and the like — does NOT ship in the AIND plugin. It lives in a separate, composable `dev-standards` plugin** (org-flavoured — e.g. one installed alongside AIND across all of an org's repos), shipping **skills** (reusable knowledge an agent pulls in), **not** flow commands. AIND itself stays domain/stack-agnostic (D11, D23). **Consumption reuses D11's two-layer pattern:** the standards plugin is the reusable **baseline** (a `solid-principles` / `dotnet-core` skill); the project's `rules/*.md` are the thin **specialization** on top — a rule *references* the relevant skill and adds the project's own exceptions plus its "what done looks like" bar, instead of restating the principle. Because the planner's tasks already cite `rules/*.md` (D23) and the coder obeys the cited rules (D24), the flow reaches the generic knowledge **through** the project rule that cites the skill — **no new flow contract is added**. **Onboarding (D18) wires the link:** when it detects a stack (e.g. .NET), it drafts rules that cite the companion skills and reports the companion plugin as a setup prerequisite. **Granularity:** start with a **single org-level standards plugin** holding stack-scoped skills; splitting into per-stack plugins is a later option if an org's stacks diverge enough to warrant it (a packaging detail, left open like other tuning calls). | Bundling stack knowledge into AIND would directly contradict the agnostic identity the planner and intake are built on (D11 "criteria-agnostic"; D23 "never hardcode a project's domains") and the portability rule (no stack values in the plugin): a Python team would carry .NET dead weight, and a single SOLID tweak would churn the *flow* plugin's version. Generic guidance sits on the **knowledge** side of the flow-vs-knowledge split — the same side as project rules; the only thing separating it from a project rule is **cross-project reusability**, which is exactly what makes it a *composable plugin* rather than AIND-core or per-project config. Plugins compose (both Claude Code and Copilot load several), so AIND + dev-standards + the project's own `.claude` stack cleanly, each versioning on its own cadence. Reusing D11's shipped-baseline + project-extension shape avoids inventing a new mechanism and keeps the project rule the single thing the flow cites — the standards skill is *referenced*, never duplicated into the flow. Shipping skills (not commands) matches "what can be scripted/skilled should be" and keeps commands reserved for the flow. Wiring it through onboarding turns "get my team started" into one documented install plus auto-cited rules, with zero stack knowledge baked into AIND. Starting with one plugin avoids premature fragmentation; the per-stack split stays available as a later packaging choice. | 2026-06-30 |
| D26 | Build phase — code reviewer (Phase 4; follows D24) | **The code-review loop is built as a cold reviewer subagent driven from inside `/aind:implement`.** The reviewer is the flow's first `agents/` occupant (`agents/reviewer.md`, `name: aind-reviewer`); `/aind:implement` spawns it (via the `Task` tool) immediately after it opens the code PR, so the **coder↔reviewer loop lives inside the warm implement session** — the coder-fix half is *warm authoring* (only that session has hot context on what it wrote), while the reviewer stays **cold** because a Task-spawned subagent gets a fresh context handed only the work-item id + PR number, never the coder's transcript (satisfies the artifacts-only re-grounding contract). **Loop:** up to **3 reviewer passes**, each a fresh cold subagent re-grounded from artifacts (PR diff+body+`AIND-LINKS` via `aind-review-pr.sh fetch`; the merged plan; **every** `.claude/rules/*.md` **and** relevant `.claude/skills/`; on re-passes, the prior open threads + coder replies); early-exit the moment a pass is CLEAN; between passes the warm coder **fixes or rebuts** each blocking finding, commits, pushes. **Severity is three-tier and the gate is deliberately strict — CRITICAL *and* WARNING both block; only SUGGESTION is non-blocking.** Convergence is protected by an objective/taste split: WARNING is an *objective* rule/threshold/skill-pattern violation with a concrete fix, anything taste-level is a SUGGESTION (never gates). **What it checks:** plan-vs-implementation (every AC, every Definition-of-done item, the task breakdown, and data contracts on the wire) **plus the full project rule + skill set** — an asymmetry with the coder, which obeys only each task's *cited* rules (D23/D24) whereas the reviewer reads them **all**, because a cross-cutting rule can be violated by a task that never cited it — plus **scope-creep as a finding** (inverting the reference planner's "don't penalise unplanned features", per D23's simplicity bias), cross-cutting concerns (auth/logging/error-handling/security/perf), and correctness/edge cases; every finding cites `file:line` + the source it violates. **Two output channels from one run:** a structured verdict returned to the orchestrator (drives the loop) and PR artifacts (resolvable inline threads for each CRITICAL/WARNING; the SUGGESTION list in the review summary body). **The reviewer authors no fixes** — no `Edit`/`Write` and it never runs `git commit`/`push` (the guarantee is structural, since `Bash` is present for `gh`). **State (D4):** the tag stays `In implementation` for the whole loop — the PR owns all iteration and a coder↔reviewer *disagreement* never moves the tag (distinct from a stuck-state). **Escalation:** after 3 deadlocked passes → a human tiebreak — a PR "human tiebreak needed" summary **and** a signed ADO work-item comment (the orchestrator posts it, signed as a new `reviewer` agent name), **tag unchanged**; a genuine reviewer *failure* (can't resolve the plan, `gh` fails) is instead a stuck-state → `Needs attention` (D12). **New mechanics:** `scripts/aind-review-pr.sh` (`fetch`/`post`/`digest`/`resolve`/`reply`); `/aind:implement` gains `Task` in its allowed-tools + the review-loop phase. **Prior art:** adopts three-tier severity, a skill-pattern-compliance dimension, and reviewer constraints from an external SENTINEL/AUDITOR reference; **rejects** its `state.json`/`plan_hash` resume (AIND's durable state is the PR + its open threads), its multi-agent `phase_outputs` handoff (one coder, D24), and a weighted 0–100% scored-audit report for the inline gate (too assertive for the suggest-don't-assert gate; a standalone `/aind:audit` is noted but descoped). **Scope ends at reviewer-approval or human-tiebreak** — no merge, no terminal `Implementation complete` (D13), and no test authoring (D8/D9); those remain the next iterations. | Packaging follows the warm/cold line D20/D24 drew: the reviewer is the independent **check**, so it is cold (a subagent in `agents/`, re-grounded from artifacts) — and coldness is what the gate is *for* (D7 Phase 4); the coder that fixes is *authoring*, so it stays warm. Spawning the cold subagent *from* the warm session does not break coldness, because a Task subagent's context is fresh and receives only artifacts — so the loop can sit inside `/aind:implement` (where the fix half must be, to stay warm) without leaking the coder's reasoning into the check. A standalone `/aind:review` would force the fix half to run cold, contradicting warm authoring, and the human-tiebreak re-run it would enable is itself a next-iteration (merge) concern. The strict CRITICAL+WARNING gate answers "really challenge the coder", made safe from nit-deadlock by the objective/taste split. The reviewer reading the whole rule+skill set (vs. the coder's cited-only) is the point of an independent check — it catches the cross-cutting violations a task-scoped view misses. Keeping the tag at `In implementation` and signalling a deadlock via *comment* (never the tag) preserves D4's coarse/fine split and the D7/D12 disagreement≠stuck distinction while still satisfying "visible in ADO". All mechanics live in a script (commands stay thin), and routing the reviewer's `gh`/ADO calls through scripts avoids the fresh-subagent permission re-prompt friction that D20 identified. Stopping at PR-review keeps this a single reviewable surface (the name-what's-available discipline of D15), with merge/complete/tests as clean follow-ons rather than half-built now. | 2026-07-01 |
| D27 | Build phase — merge gate + terminal completion (Phase 4 close-out; realises D13) | **The build phase closes out with a human-run `/aind:complete <work-item-id> [pr-number]` — the build-phase twin of `/aind:approve-plan`.** It resolves the story's code PR, **verifies it is MERGED (refuses otherwise)**, writes the terminal `Implementation complete` tag, posts a signed completion note, and cleans up the merged code branch — in that order. **D13 realised as verify-then-tag, NOT command-merges:** the human merges the PR in GitHub (the human is the final authority on what lands), and `/aind:complete` *records* the result — it does **not** run `gh pr merge`. This still honors D13's committed safety invariant (the terminal tag is only ever written *after* a confirmed merge, so `Implementation complete` can never sit on an unmerged PR — the state-record lie D4 guards against; verify-then-tag satisfies "merge FIRST, then tag" trivially), and matches `/aind:approve-plan`'s confirm-merged→record shape exactly, keeping the command thin and free of merge-method/branch-protection surface. The residual drift D13's one-command-does-both was meant to prevent (a human merges but forgets to run `/aind:complete`) is the **same accepted gap already tolerated for `/aind:approve-plan`**, and D13 already defers the real close (auto-merge-on-approval + a merge Action) to the automation phase (D6). **PR discovery (the one new mechanic):** the code branch is coder-generated and non-derivable (D17), so `/aind:complete` *searches* the repo's PRs for the work-item id and filters to AIND's own markers — a title ending `(AB#<id>)` (produced by `aind-open-code-pr.sh open`) or an `AIND-LINKS work-item:` URL ending `/edit/<id>` — requiring **exactly one MERGED match**; it **refuses** on none-merged ("merge it first"), no-match, or ambiguity, and takes an optional explicit **`[pr-number]`** override so it never *guesses* which PR (suggest-don't-assert). **No hard reviewer-approval gate:** there is no "reviewer approved" tag (the tag stays `In implementation` through the review loop), so the **merge itself is the human's act of acceptance** — mirroring how `/aind:approve-plan` trusts that the human merged only after resolving threads. **Order + recoverability (D13):** verify → `aind-status.sh "Implementation complete"` → signed `coder` completion note → branch cleanup **last**; nothing mutates before the merge is confirmed, so a tag-write failure leaves the item *recoverable* (`In implementation`), never false-complete, and a cleanup hiccup can never corrupt status. **Branch cleanup tuned for auto-delete-on-merge:** since GitHub usually auto-deletes the remote branch at merge, cleanup re-confirms MERGED then deletes the remote **only if still present** (no-op otherwise), **prunes** the stale `origin/<head-ref>` ref, and deletes the **local** branch (switching off it to the integration branch first, only when the working tree is clean; dirty → warn, leave it) — the local branch being what actually lingers from the `/aind:implement` run. **New mechanics:** `commands/complete.md` + `scripts/aind-complete.sh` (`verify`/`cleanup`). **Refinement (2026-07-01):** cleanup also leaves the developer on an up-to-date integration branch — after deleting the merged code branch, if the checkout ends up on the integration branch with a clean tree it **fast-forwards it to include the merge** (ff-only; a diverged local branch is left for a manual pull), so the local trunk isn't silently behind the just-merged work. **Scope: `/aind:complete` alone this iteration; the code-revision loop is deferred** (the twin of the plan-revision loop D21 — re-entering an already-open code PR to apply a wanted suggestion / a human tiebreak verdict / a touch-up, pushing to the same PR, optionally re-reviewing). Safe to defer because the common close-out path (CLEAN → human merges as-is → `/aind:complete`) needs no loop, and the suggestion/tiebreak sub-cases have a sanctioned interim bridge (a direct edit on the PR branch + push, then merge — a human decision at merge time). Still out: test-writer/E2E (D8/D9), dreaming (D16), automation (D6). | The realistic answers for manual scope collapse to human-gated + a thin CLI recorder, and `/aind:approve-plan` already proved that exact shape one phase earlier — reusing it (rather than D13's literal command-merges) keeps `/aind:complete` thin, avoids merge-method/branch-protection judgment the tool shouldn't own, and still guarantees D13's only load-bearing property: the terminal tag never precedes the merge. Verify-then-tag makes the *only* possible failure a recoverable, visibly-incomplete item, never a false-complete one — a lie in the state record is far worse than a missed update (the D4 discipline). Searching for the PR (vs. reconstructing a branch) is forced by D17's "reach the branch through the PR"; requiring a single MERGED match with an explicit override keeps discovery from ever guessing. Not gating on reviewer-approval respects that the merge *is* the human's acceptance (there is no approval tag to check) and mirrors the plan phase. Tuning cleanup for auto-delete-on-merge reflects the real post-merge state — remote gone, local branch lingering — so the command tidies what's actually left instead of erroring on an already-deleted remote. Shipping `/aind:complete` alone keeps a single reviewable surface (D15) and leaves the code-revision loop as a clean, well-scoped follow-on rather than a half-built bundle; the interim hand-edit bridge covers the only paths the loop's absence blocks. | 2026-07-01 |
| D28 | Build phase — code-revision loop (steer the coder from the PR; implements D27's deferred twin of D21) | **`/aind:implement` becomes mode-aware — the twin of the plan-revision loop (D21): a first run BUILDS the code PR; a re-run on a story that already has an open code PR enters REVISE mode**, so a human can *steer the coder from the PR*. Revise mode: resolve + check out the open code PR's head branch, print the **steering digest** (top-level PR comments + every review thread tagged `[OPEN]`/`[RESOLVED]` with `thread=<id>` and `file:line`, plus the reviewer's summary/SUGGESTIONs), apply **only the human-directed** changes, reply on each acted thread, commit + push to the **same** PR, then (by default) re-enter the code-review loop. **Three needs, one capability:** applying a wanted reviewer SUGGESTION the human picked after a `CLEAN` verdict, executing a human's **tiebreak verdict** after a 3-pass deadlock (Phase 4's "coder executes the verdict → one more reviewer pass → merge"), and any pre-merge touch-up — all reduce to re-entering the open PR to make directed changes. **Decisions:** (1) **entry = re-run `/aind:implement`** (mode auto-detected), not a new command — the fix half is *warm authoring* and belongs in the warm coder session (D20/D24), exactly as `/aind:plan` folds revise into the same command; (2) **autonomy = only what the human directed** — the coder applies the picked suggestion / tiebreak verdict / requested touch-up (plus any CLI instruction) and does **not** auto-apply undirected SUGGESTIONs or free-re-implement (suggest-don't-assert: the human decides *what*, the coder executes); (3) **re-review = on by default, skippable for a trivial touch-up** — after the push the coder re-enters the existing bounded (≤3-pass) review loop, matching Phase 4's post-verdict pass, but the human may say *skip* for a directed touch-up and merge directly. **State (D4):** the tag stays `In implementation` for the whole loop — a revision is more PR iteration, never a tag move; the coder **replies** on threads but **never resolves** them (resolution stays the human's merge gate, twin of D21). **Scope stops before merge** — `/aind:complete` (D27) is still the only terminal step; a plan-level problem surfaced during revision is **flagged for a human**, never silently re-planned or rerouted. **New mechanics:** `scripts/aind-revise-code-pr.sh` (`status` = detect an OPEN code PR by marker-search; `begin` = dirty-tree guard → checkout head branch → print the steering digest via `aind-review-pr.sh digest`; `push` = push the code branch to the same PR + optional comment) — thread replies reuse `aind-review-pr.sh reply`. The code-PR **marker-search** (title `(AB#<id>)` / `AIND-LINKS` work-item id) is factored out of `aind-complete.sh` into a shared `aind-common.sh` helper (`aind_find_code_prs`) that both callers reuse, each applying its own state policy (complete wants a single MERGED match; revise wants a single OPEN). `aind-open-code-pr.sh`'s re-run refusal now **points to revise mode** instead of "not supported". | The plan phase already proved this exact shape one phase earlier (D21): mode-aware re-run, same-PR/same-branch iteration, agent-replies-but-never-resolves, tag unmoved — reusing it for code keeps the two build/plan loops symmetric and within v0 manual scope (no automation added). Re-running `/aind:implement` (not a new command) keeps the warm/cold line clean: applying feedback is authoring, so it stays warm; the reviewer stays the cold subagent spawned exactly as before. Restricting the coder to human-directed changes is the whole point of "steer from the PR" — it makes the human the decision-maker and keeps merge-time behaviour predictable (an agent that swept up every undirected suggestion would re-assert taste the human deliberately declined). Default-on re-review closes the tiebreak path Phase 4 specified, while the touch-up skip avoids re-opening the gate on a trivial change the human already owns. Non-derivable branch (D17) forces PR-by-search, so factoring the marker-search into one helper avoids two divergent copies (DRY; the reviewer would flag the duplication) and is safe because `aind-complete.sh`'s selection is covered by unit tests re-run after the refactor. Closing the re-run refusal turns D27's interim "hand-edit the branch" bridge into a first-class, reviewable flow. | 2026-07-01 |
| D29 | Signed GitHub PR comments (bug fix; extends the D2/D26 signing rule to the GitHub side) | **Every GitHub PR comment, review thread, and thread reply an agent posts is signed with the agent name — the same attribution ADO comments have carried since the start.** Signing was originally built only into `aind-comment.sh` (the ADO path); every PR-side posting site (`aind-thread.sh`, `aind-review-pr.sh summary`/`thread`/`reply`, `aind-revise-code-pr.sh push`, `aind-revise-plan-pr.sh push`/`reply`) posted the raw body, so on GitHub a reviewer finding and a coder rebuttal were indistinguishable. Fix: a shared `aind_gh_signature <agent>` helper in `aind-common.sh` appended by each posting site; the multi-author scripts (`aind-thread.sh`, `aind-review-pr.sh`) take an explicit `<agent>` positional (planner assumptions, reviewer findings, coder rebuttals), while the single-author revise scripts hardcode `coder`/`planner`. The machine marker on GitHub is a **real HTML comment** `<!-- AIND-AGENT: <name> -->` (GitHub preserves HTML comments and renders markdown), not the `display:none` span ADO needs (ADO strips HTML comments) — same greppable `AIND-AGENT: <name>` contract, host-appropriate carrier. **PR bodies stay unsigned** (open-plan-pr/open-code-pr): a body is the artifact, authored once, and already carries the `AIND-LINKS` marker. **No new hook:** there is no PreToolUse guard on the PR path yet (unlike ADO's `check-claude-comment.sh`) — signing holds because every PR-comment site already routes through these scripts; adding a raw-`gh pr comment` guard is a noted follow-up. | The signing rationale (attribution when every agent posts under one identity, D2/D26) applies verbatim to GitHub PRs: in local/v0 mode the coder, reviewer, and planner all post under the developer's single GitHub login, so without a signature the PR conversation loses track of who said what — the exact failure ADO signing was built to prevent, simply never carried across to the GitHub side when the build phase (D24/D26/D28) added PR posting. One shared helper (not per-script duplication) keeps the two hosts' signatures consistent and is where the host difference (HTML-comment vs display:none marker) is localized. Passing `<agent>` explicitly to the multi-author scripts mirrors `aind-comment.sh`'s `<agent>` arg and avoids guessing the author; hardcoding it in the revise scripts avoids interface churn where there is only ever one author. Leaving PR bodies unsigned matches their once-authored nature and avoids noise. Deferring a PR-side enforcement hook is safe for now (all sites route through the scripts) and mirrors how ADO enforcement was added after the signed path existed. | 2026-07-06 |
| D30 | Dreaming phase — implementation (realises D16; pins the details D16 left open) | **The dreaming phase (D16) is built: emission via a signed script, a git-native lessons store, a manual `/aind:dream` cycle with a cold dreamer subagent, and two curation gates.** Concrete choices, each an item D16 deliberately left to implementation: **(1) Store = git, on a dedicated orphan branch** `aind/lessons` (default; `AIND_LESSONS_BRANCH` optional override, GitHub-side). It never merges into integration — a pure append-only exhaust store — so lesson records never pollute a story's diff, need no branch-protection exception, and live in the same medium (git/PR) as the dreamer's output. Chosen over ADO (status lives there; config-improvement fuel belongs with config in git) and over co-locating lessons in the work PR (intake owns no branch and the cold reviewer can't commit; and a lesson gated on story-merge can't feed a cross-story cadence). **(2) Emission = one signed script** `aind-emit-lesson.sh`, called by each agent at session end — the exhaust twin of `aind-comment.sh`. It writes the record via **throwaway-index git plumbing** (`commit-tree` + `update-ref` + push, no checkout), so an agent emits mid-session without being pulled off its branch. **Human PR feedback becomes lessons through the agent that acted on it** (planner/coder revise runs already read the threads → emit `correction`/`suggestion` sourced to the thread), which realises D16's deferred human-override capture *without* a friction-adding prompt at the human gate — the tiebreak verdict the coder executes is captured as a `correction`. **(3) Record schema** = front-matter (`id`, `work_item`, `agent`, `phase`, `severity`, `source`, optional `area`) + an **Observation** body only. `severity` is a 4-value enum keyed to *degree of human intervention* — `observation`/`suggestion`/`correction`/`blocker` — the highest-value signal. **Deliberately excluded:** a *frequency* field (recurrence is the dreamer's cross-story computation, not knowable per-record) and any *suggested-fix* field (the remedy is exclusively the cold dreamer's — a pre-baked fix would anchor/contaminate the cold synthesis; the "why" the agent uniquely knows goes in the Observation instead). Lifecycle is **directory-based** (`new/` → `archive/` or `rejected/`), the single source of truth (no competing status field). **(4) Cadence = manual `/aind:dream` now, scheduled later** — the correct v0 shape (matches the manual scope of the whole flow); the *same* command becomes the scheduler's handle when an automation layer exists. **(5) Threshold = a rubric, not a counter:** the dreamer judges each cluster on **severity × recurrence × factualness** (a lone `blocker`/`correction` or a verifiable defect is actionable at n=1; a taste cluster needs recurrence), and **surfaces borderline clusters with a confidence label** rather than silently dropping them (the human prunes at review). A pure count-gate was rejected (cold-start starvation; a single human-abort is already worth acting on; the missing-eslint defect is actionable on first sight). **(6) Curation = two gates, dream-first-then-curate-clusters:** the cold dreamer clusters + judges *first* (analysis pass, no edits), the human curates the **clusters** (approve/adjust/reject — *with* the recurrence view in front of them), then the dreamer authors only the approved clusters as `.claude` edits (author pass), and the config PR is Gate 2. Curating clusters (not raw lessons) fixes the blind-spot where a human would reject a lone lesson that was actually a pattern, and is less tedious. **(7) Scope = the project's `.claude`, scope-by-default with principled carve-outs** — **Refinement (2026-07-07):** an early draft used a hardcoded folder *allowlist* (`rules/`, `skills/`, `intake-rubric.md`, project `agents/`, imported `CLAUDE.md` prose); that is brittle — it goes stale the moment a project keeps a convention in `.claude/scripts/` or `.claude/hooks/`, making a legitimate lesson about it un-actionable. Inverted to **scope = any behavior file under the project's `.claude/`** minus four carve-outs the dreamer must **never** edit (→ parking-lot instead): (a) the **flow** — status model, gates, phase sequence, and the "AIND operational rules" in `CLAUDE.md`; (b) its own **guardrails** — `settings*.json` (permissions) and any *enforcement* hook/script (signing, tag-writing), told from a benign dev hook/script by **purpose, not name**; (c) **secrets** — `aind.env`; (d) anything **outside `.claude/`** — product code, CI, infra. This is broad enough to fix a wrong build command wherever it lives (a skill *or* a project script) yet not loose: the bright line is flow+guardrails+secrets+product-code, and the permanent human PR gate backs it. Generic reusable knowledge is still a **parking-lot note** pointing at the companion standards plugin (D25), not a cross-repo PR. A suspected flow problem is a parking-lot note (`aind-dream.sh note` → durable `.aind/parking-lot.md`), never a diff. **Cold/warm split:** analysis (the judgment that must be independent) *and* authoring both sit in the cold `agents/dreamer.md` (the human gate sits between them); `/aind:dream` is the warm orchestrator that spawns it (twice) and owns the git/gh + curation mechanics — the dreamer is the one cold agent that legitimately holds `Edit`/`Write` (its output *is* a diff), an intentional asymmetry with the reviewer (a check that must never write). **New artifacts:** `scripts/aind-emit-lesson.sh`, `scripts/aind-dream.sh` (`digest`/`start`/`open-pr`/`consume`/`note`), `agents/dreamer.md` (`analyze`/`author` modes), `commands/dream.md`; `aind-common.sh` gains `aind_lessons_branch`/`aind_lessons_ref`/`aind_lessons_push`; emission wired into `intake`/`plan`/`implement`. Offline-validated (throwaway-index plumbing: emit/digest/consume/note leave the working tree untouched); live-validation pending on a testbed where a `lint` skill references an uninstalled eslint (the end-to-end probe: agent emits the defect → `/aind:dream` proposes the skill fix). | D16 fixed the *shape* (reflect-warm/synthesise-cold, one-PR-per-cycle, permanent human gate, config-not-flow) but left the store, record schema, cadence trigger, and pattern threshold explicitly open as tuning/implementation details — this decision closes them with the least-surprise choices given the rest of the system. Git-on-an-orphan-branch is the only store that is durable, cross-machine readable by a *cold* agent, decoupled from story-merge (so a cross-story cadence works), and in the same medium as the output PR — while keeping meta-exhaust out of product diffs. The signed-emit-script reuses the exact "mechanics-in-one-signed-script" discipline of `aind-comment.sh`/`aind-status.sh`, and the throwaway-index plumbing is what lets emission be a *session-end side effect* rather than a branch-disrupting operation. Capturing human PR feedback through the acting agent (rather than a gate prompt) is what makes D16's highest-value-but-deferred datum fall out for free. The schema exclusions are load-bearing, not minimalism: a per-record frequency would be wrong (only the dreamer sees across stories) and a per-record fix would defeat the coldness the whole phase is built around (the dreamer must originate the remedy after seeing the pattern, not rubber-stamp a warm agent's local guess). The severity-as-human-intervention enum turns the "why did a human override" signal into something groupable. Manual cadence is not a compromise but the honest v0 answer (same manual scope as D6), and it costs nothing to schedule later. The rubric-threshold (not a counter) avoids cold-start starvation and respects that a single abort or a verifiable defect is already actionable, while confidence-labelled surfacing (not silent dropping) keeps the human in control of the borderline. Dream-first-then-curate-clusters is strictly better than curate-raw-then-dream: it gives the human the recurrence view *at* the curation moment (so they don't reject a lone lesson that was a pattern) and triages ~clusters instead of ~lessons. Confining scope to the project `.claude` keeps one repo / one PR and honours the D16 boundary; parking-lot-for-generic-knowledge respects D25's separate standards plugin without inventing cross-repo PR machinery on day one. The dreamer holding `Edit`/`Write` is the defensible asymmetry with the reviewer: the reviewer must never write because writing would compromise its check; the dreamer's *entire deliverable* is a proposed diff, and its independence is preserved structurally (re-grounded from lessons + current config, spawned cold from the warm orchestrator, gated by two human reviews and the config-not-flow boundary). | 2026-07-07 |
| D32 | Intake (phase 0) — dependency-implementation gate (bug fix; extends D2's intake gate) | **Intake now validates that a story's *dependencies are actually implemented*, not merely *named* — and an unmet dependency declines the story without touching the readiness score.** The bug: intake read the story text (rubric O5 checks only that dependencies/blockers are *named or explicitly "none"*) but never inspected the **linked** predecessor stories' status, so a well-formed story could pass intake and enter planning while a story it depends on was still unbuilt — exactly the sequencing failure the gate should prevent. Fix: a new deterministic script `scripts/aind-deps.sh <id>` resolves the item's **Predecessor** links (ADO `System.LinkTypes.Dependency-Reverse` — "must be completed before this one"), fetches each target's title + ADO state + AIND status tag, and classifies it `IMPLEMENTED` / `NOT IMPLEMENTED` / `UNKNOWN` — *implemented* = AIND status `Implementation complete`, or (for a dependency not tracked by AIND) a done-like ADO state (Closed/Done/Resolved/Completed) — emitting a final `DEPS_VERDICT: NONE|MET|UNMET` line the command reads. **Gate semantics chosen to match the report:** an `UNMET` verdict (any dependency not implemented or unreadable) → **`Intake declined`**, *even at readiness score 100* — the gate is orthogonal to the rubric and **never enters the score formula** (the score reflects story-text design quality; the verdict is the sequencing gate). It is a **command-level structural gate, not a rubric criterion** (like the rubric-missing guard and the past-intake-tag check already are), so it is not scored and adds no row to the score table — it gets its own **Dependencies** section in the verdict comment naming the unmet stories. A story declined *solely* for an unmet dependency **needs no text edit**: it becomes ready when the dependency lands and the human re-runs `/aind:intake` (the report says so, so the author doesn't hunt for a non-existent story problem). **Direction is load-bearing:** only Predecessor (Dependency-Reverse) links gate — a Successor (something that depends on *this* story) does not, and Hierarchy/Related/ArtifactLink relations are ignored. **New mechanics:** `scripts/aind-deps.sh`; `commands/intake.md` gains step 4 (the gate, between scoring and posting) and the comment's Dependencies section. Offline-validated (jq predecessor-extraction picks only Dependency-Reverse from a mixed-relation fixture; the tag-value / done-state / normalize helpers unit-checked); live-validation pending against a story with a linked, unfinished predecessor. | The rubric was never the right home for this: rubric criteria are *scored* and feed both the verdict and the readiness number, but the user requirement is explicitly that a perfectly-defined story **scores 100 yet is declined** when a dependency is unbuilt — a scored criterion can't express "hard-block but don't dock points". So it belongs where the rubric-guard and tag-checks already live: a command-level gate that the LLM applies from a deterministic script's verdict, keeping "what can be scripted is scripted" (the LLM never eyeballs relations JSON). Keying *implemented* to the AIND terminal tag with an ADO-state fallback covers both AIND-tracked and externally-tracked dependencies without config. Gating on Predecessor-only is the correct semantic (a successor depending on this story says nothing about *this* story's readiness) and treating UNKNOWN as blocking is fail-safe (can't confirm implemented ⇒ don't let it through). Leaving O5 intact keeps the two checks complementary — O5 catches a story that never *declares* its dependencies (a text gap the author must fix); the new gate catches declared-and-linked dependencies that aren't *done* yet (a sequencing state, no edit needed). | 2026-07-08 |

---

## Open questions

*None open.* Every question raised during the design (Q1–Q10, plus the dreaming-phase questions) has been resolved by a decision or closed by descoping — see the lists below. Remaining future work lives in the **parking lot** (deployment, cost/budget, story decomposition, multi-agent splitting, plan-triage) and in the **deferred-until-automation** items called out in the decisions (the service-identity question, automation-mode merge, automated E2E). One **dreaming sub-item** is deliberately deferred: capturing the human decider's/merger's override reason (the human is not an agent and does not auto-emit) — see D16.

*Resolved:* Q1 (loop-back model) → **D1**. Q2 (triggers) → **D6**. Q3 (plan PR shape) → **D10**. Q4 (readiness rubric) → **D11**. Q5 (review responsibility split) → **D7**. Q6 (exception / escape hatches) → **D12** (generalized stuck-state protocol; the coder↔reviewer deadlock sub-case was already **D7**, and the v2 staleness sweep remains deferred under **D6**). Q8 (merge mechanics) → **D13** (human-gated merge + CLI tag-write in manual scope; automation-mode merge deferred). Q9 (behavioral suite shape) → **D14** (same-branch, reviewed in place; principled divergence from D10). Q10 (E2E execution environment) → **D15** (manual developer-run path now; automated E2E deferred to the automation + deployment stories). Dreaming-phase questions (collection model, cadence, output surface, scope of authority) → **D16** (hybrid emission with cold synthesis; PR-against-`.claude`, one cycle = one PR; permanent human gate; authority bounded to the config layer).

*Closed without a decision (descoped):* Q7 (agent identity & secrets) — **closed by the D6 amendment (2026-06-24) scoping near-term execution to manual-only**, which removes any unattended agent needing a service identity. Not answered, just no longer applicable in current scope; **reopen when GitHub Actions automation is picked up** (single bot vs. per-agent, PAT vs. Entra service principal + OIDC, secret storage all still to be decided then).

---

## Parking lot

- Deployment pipeline specifics (out of scope for initial design)
- **Testing strategy — both halves now designed.** Point 1 (TDD / behavioral spec-tests) → **D8**; point 2 (live / end-to-end testing) → **D9**. Remaining testing-related open items live in the open-questions table: persistently-red suite handling (Q6), behavioral-suite branch/PR shape (Q9), and the automated-E2E execution environment (Q10).
- Multi-agent / parallel Claude Code sessions — including splitting the single coding agent or single reviewer agent into multiple skill-specific agents (project-specific; D7 models each as one for now).
- Cost / token budget considerations
- Atomic unit / story decomposition — is one US = one branch = one PR? Should the planner fan large stories into task-level PRs? Ties to ADO hierarchy.
- **Dreaming phase — designed (D16) AND built (D30).** The continuous-improvement loop is implemented: emission via `aind-emit-lesson.sh`, a git orphan-branch lessons store, a manual `/aind:dream` cycle with the cold `aind-dreamer`, and two curation gates. The items D16 left open are now pinned by D30: the **lessons-record schema** (front-matter + Observation, severity-as-human-intervention enum), the **store** (git orphan branch), the **cadence** (manual now, scheduled later), and the **pattern-vs-one-off threshold** (severity×recurrence×factualness rubric, not a counter). The **human-override capture** D16 deferred is now realised for free — human PR feedback becomes a `correction`/`suggestion` lesson via the agent that acted on it (no gate prompt). Remaining: **live end-to-end validation** (the missing-eslint `lint`-skill testbed probe); and the D25 companion-standards-plugin path is still a parking-lot note, not a cross-repo PR.
- Plan-triage step (an automated quality bar on the plan *before* human review) — deferred; human review judged sufficient for v1.
