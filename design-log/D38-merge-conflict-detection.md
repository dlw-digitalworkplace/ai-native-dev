# D38 — Build phase — merge-conflict detection + a rebase resolution path in the review loop

- **Area:** Build phase — merge-conflict detection + a rebase resolution path in the review loop (relates to D26, D33, D37)
- **Date:** UNKNOWN` (D36 adapter — never call `gh`/`az` directly), surfaced through `aind-review-pr.sh` (the reviewer's one allowed Bash) in `fetch` and a new polling `mergeability` phase. Because both hosts compute mergeability **asynchronously**, `UNKNOWN` is polled briefly and then treated as **non-blocking advisory** (only a confirmed `CONFLICTING` blocks) so a transient recompute never deadlocks the loop. The conflict finding has no `file:line`, so it is the one finding documented in the **summary** (not an inline thread) under the synthetic locus `merge:integration`. **Resolution fixes a recorded lesson (57-coder):** `aind-revise-code-pr.sh` gains a `rebase` phase (fetch integration, rebase the PR head, leave conflicts in the tree for the coder to resolve in-context) and its `push` becomes **force-with-lease-aware** (a rebase rewrites history, so a plain push is rejected non-fast-forward — the push now detects the diverged remote via `merge-base --is-ancestor` and force-with-leases only then, plain fast-forward otherwise). **Kept reactive, not proactive** (the coder does *not* pre-emptively rebase before every push) — smallest change matching the observed flow; prevention (auto-rebase to reduce conflict frequency) is named-not-built. **Offline-validated then live-validated end-to-end (2026-07-17)** on a real conflicting PR: the reviewer flagged `CONFLICTING`, the coder rebased onto integration and force-with-lease-pushed, and the re-review returned `CLEAN`. Distinct axis from D22 (agent host) and D36 (code host); the status model, gates, and PR contract (D4/D5/D13/D17) are untouched — this is loop mechanics on the config side of the D1–D15 line.
- **Status:** Active

## Decision
**The cold reviewer now checks, every pass, whether the code PR still merges cleanly into the integration branch, and flags a conflict as a blocking finding; the coder clears it by rebasing onto integration and force-with-lease-pushing, then hands back for a fresh pass.** Parallel work via worktrees (D37) makes this a real, recurring case: a PR that was mergeable at build time goes **conflicting** the moment another PR merges under it, and nothing in the loop noticed. **Detection is a PR *read*, not a run** — mergeability is a property the host computes (`gh` `mergeable`; ADO `mergeStatus`), so it fits the reviewer's cold, read-only, "review-by-reading-not-executing" contract (D35) exactly: a new forge verb `forge_pr_mergeable` normalises both hosts to `MERGEABLE

## Rationale
CONFLICTING
