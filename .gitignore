# Build artifact produced by deploy.sh (uploaded as a Release asset, not committed)
aind.zip

# Project-side secret (never in this repo; listed defensively)
.claude/aind.env

# Local, per-developer Claude Code permission grants (not shared, not shipped)
.claude/settings.local.json
